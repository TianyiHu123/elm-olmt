# Summaries

Store compact aggregated result files here (JSON/CSV), organized by iteration:

- `iter001/`
- `iter002/`
- ...

Do not store large raw run artifacts here. Keep those in `/pscratch/.../UQ_output/...`.
