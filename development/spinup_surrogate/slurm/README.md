# Slurm Scripts

Keep iteration-specific Slurm scripts here:

- `iter001/`
- `iter002/`
- ...

Guidelines:

- Keep one canonical script per iteration and annotate variant usage.
- Prefer parameterized scripts with environment-variable variant selection.
- Record the script path used in `iterations/iterXXX.md`.
